import { ContextSource, DecisionItem } from '../types';

// Mock datasets removed. Trace uses only live synced Google Workspace sources.
export const INITIAL_CONTEXT_SOURCES: ContextSource[] = [];
export const INITIAL_DECISIONS: DecisionItem[] = [];
